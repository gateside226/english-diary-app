# ローカルで `vercel dev` を使う場合、このファイルを .env.local としてコピーし、
# 実際のAPIキーを設定してください（.env.local はGitにコミットされません）。
# 本番環境（Vercel）では、ダッシュボードの Project Settings > Environment Variables で設定します。

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
